# README screenshots

The main README references two captures from a live run. Add them here with these names:

- `review-queue.png`: the operator review UI showing a paused run awaiting approval.
- `trace-explorer.png`: the trace explorer's status-coloured span tree, a node expanded to
  show the nested cross-worker spans and per-node cost.

Until they're added, the README's screenshot links show as broken images on GitHub.
